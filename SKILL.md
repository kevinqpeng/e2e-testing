---
name: e2e-testing
description: 按用户指定业务场景进行端到端测试、跨端联调、回归验证或完整业务生命周期验收。理解规则与调用链，复用或编写固定脚本，使用 ego-browser 操作网页，按需用 Jev 判断语义；分析失败并在授权范围修复、复测、输出证据报告。适用于任意项目，可全局或项目安装。
license: MIT
compatibility: Requires Node.js >=22.20.0 and ego-browser on PATH for the bundled browser runner. Jev MCP or TypeSafe API is needed only for semantic assertions.
metadata:
  author: kevinqpeng
  version: "0.1.0"
---

# E2E Testing

主模型负责理解业务、编写用例和诊断修复；固定脚本控制流程与硬断言；ego-browser 操作网页；Jev 处理确需语义理解的窄问题。

## 安装位置与依赖

本技能是一个完整目录：SKILL.md、scripts、references、examples 一起安装。全局安装和项目安装内容完全相同，不互相引用，也不依赖其他业务仓库。

以本次实际加载的 SKILL.md 所在目录为技能根目录。运行该目录的 `scripts/runner.mjs`，阅读相对路径 [用例与结果契约](references/scenarios.md)。禁止把开发者的绝对路径写进项目；项目安装不回退到全局路径。需要队友复现时采用项目安装，并提交技能目录与安装器锁文件。

使用浏览器前读取已安装的 ego-browser 技能，按其真实 API 和控制权规则操作。缺少运行时则报告缺失依赖，继续独立可完成的源码与用例准备。技能安装器不会自动安装浏览器或配置模型凭证。Jev 不可用且语义断言必需时保留 INCONCLUSIVE。

## 1. 定义业务验收

1. 从当前工作区和用户指定路径定位相关项目，读取各自 AGENTS.md、版本控制状态与已有测试说明，保留已有修改。只涉及当前场景相关的端与仓库。
2. 提取目标、起点、终点、参与角色和前置条件。“整套流程”需要覆盖阶段依赖及最终结果；仅对影响执行的缺失信息提问，沿现有授权推进。
3. 核对最新契约与当前实现，沿 UI、请求、服务、持久化、后台任务和消息回调理解完整调用链。异步触发成功不等于业务完成。
4. 固定验收表：`阶段 | 角色与入口 | 前置数据 | 操作 | 预期状态与数据 | 证据`。金额、数量、资格、状态、时间范围、幂等性由代码按契约独立计算预期值。

完成条件：用户终点对应全部必要阶段与断言；不能用局部通过代表完整生命周期通过。

## 2. 复用或编写场景

1. 检查项目已有用例，匹配则复用，部分匹配则补齐，没有则编写最小固定脚本与明确入口。使用项目已有框架和依赖；不要默认运行示例或不相关的旧用例。
2. 通用 runner、判定器随本技能分发；项目只保留业务步骤、配置、断言和数据规则，无需再建一个项目包装技能。新增场景是普通 `.mjs` 模块，契约见 references/scenarios.md；无需修改全局注册表。
3. 将场景放在项目测试目录，配置不包含明文凭证；服务地址、语言版本与启动命令从该项目确定。本技能没有默认业务 URL、端口或 JDK 版本。
4. 非平凡逻辑保留最小可运行检查。语义问题与阈值必须可审阅，不能复用不相关场景的语义要求。

## 3. 执行与取证

1. 核实实际环境、各角色登录、数据库及后台任务依赖，优先复用服务。限定本次数据和清理/保留规则。沿已明确的授权执行；超出范围的真实付款、账户变更或外部副作用先取得明确授权。
2. 被测网页操作通过真实 UI 完成，API/数据库用于前置准备与独立核对。纯 API、H5、原生端、模拟回调与真实到账分别标明。
3. 按 ego-browser 规则复用本轮 TaskSpace；恢复失败任务时传 E2E_SPACE_ID。不要新建任务绕过用户接管。页面定位来自当前快照，等待可观察状态；写操作结果不明时先查询实际结果，避免重复写入。
4. 从项目根目录运行本技能内的 runner，显式指定该项目的场景与配置。输出保存在该项目的独立运行目录，不写回技能安装目录。
5. 核对页面、响应和持久化数据；异步阶段等待最终业务记录。每阶段保存必要截图和最小证据，不保存 Cookie、Token、密码或完整请求头，保留首次失败记录。

## 4. Jev 与最终状态

确定性字段直接断言。需要语义理解时，将同一证据的独立问题合并为一次真实 `jev_ask` 调用；依赖新状态的问题等取到新证据后再问。工具由主模型调用，不是 shell 命令；API 方式也须保留实际响应。

随包判定器只接受 Choice 的 `satisfied`、`violated`、`insufficient_evidence` 三种选项。其他类型需项目提供明确判定，不能直接输入此 finalizer。保存模型版本、概率及 confidence；将回应绑定到本次 runId 和请求摘要。低置信度补证据或报告不确定，不降低门槛、不重抽答案求绿。

没有语义断言且硬断言全部通过才直接 PASS；有必要语义断言则调用模型并 finalize。明确违反要求为 FAIL；缺证据、依赖异常、模型不确定为 INCONCLUSIVE。编造响应或用主模型代替 Jev 不构成验收。阈值仅为待校准起点，不是准确率保证。

## 5. 修复、清理与报告

根据证据区分产品、脚本、前置条件与环境问题。获授权修复时沿完整调用链做最小修改，使用项目自己的运行时和测试要求；修改选择器或等待时保持业务验收条件不变。重跑原失败场景与受影响的后续阶段，默认最多两轮修复后报告仍未解决的问题。

执行约定的数据清理，无法完成时说明残留标识和原因。按浏览器技能结束任务；场景负责其任务生命周期，runner 不擅自重新打开已结束的任务。

报告目标、实际环境与覆盖端、逐阶段结果、硬断言、真实 Jev 结论、证据、首次失败、修复复测、清理和未验证范围。脚本通过不证明未覆盖的业务正确。提交、推送、发布与对外消息分别遵循用户授权。
